import { Router } from 'express';
// import {
//   getTopCoinHolders,
//   getUserData,
//   getUserTokenList,
//   fetchOwnerAssets,
//   getUserSolanaBalance,
//   updateUserProfile,
//   searchUserList,
// } from '../controllers/user.controller';

// import { protect } from '../middleware/protect';

const router = Router();

// router.get('/holders/:coinAddress', protect, getTopCoinHolders);
// router.get('/assets', protect, fetchOwnerAssets);
// router.get('/solana-balance', protect, getUserSolanaBalance);
// router.get('/token-list', protect, getUserTokenList);
// router.get('/info', protect, getUserData);
// router.post('/update', protect, updateUserProfile);
// router.get('/list', protect, searchUserList);

export default router;
